const dice = require('convenient-methods-of-randomness');

const dunno = (notes) => {
  return notes + 'hello';
};

const thiser = () => {
  return 'C1 B6';
};

const pitchAlgos = {
  dunno,
  thiser,
};

module.exports = { pitchAlgos };
